{{- $repoSrc := .Values.repoSource -}}
{{- if .Values.cluster_api.enabled -}}
{{- $wave := .Values.cluster_api.wave -}}
{{- $project := .Values.cluster_api.project -}}
{{- $path := .Values.cluster_api.path -}}
{{- $selfHeal := .Values.cluster_api.selfHeal -}}
{{- $prune := .Values.cluster_api.prune -}}
{{- $allowEmpty := .Values.cluster_api.allowEmpty -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cluster-api
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: {{ $wave }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: {{ $project }}
  source:
    path: {{ $path }}
    repoURL: {{ $repoSrc }}
    targetRevision: HEAD
  destination:
    namespace: cluster-api
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      selfHeal: {{ $selfHeal }}
      prune: {{ $prune }}
      allowEmpty: {{ $allowEmpty }}
    syncOptions:
      - CreateNamespace=true
{{- end }}