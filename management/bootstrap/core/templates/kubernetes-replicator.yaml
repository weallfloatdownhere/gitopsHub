{{- if .Values.kubernetes_replicator.enabled -}}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kubernetes-replicator
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: {{ .Values.kubernetes_replicator.wave | quote }}
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    path: '{{ .Values.sourceUrl }}/kubernetes-replicator/head'
    repoURL: {{ .Values.sourceUrl }}
    targetRevision: HEAD
  destination:
    namespace: kube-system
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      allowEmpty: false
      selfHeal: false
      prune: false
{{- end }}